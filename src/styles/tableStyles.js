import { makeStyles } from "@material-ui/core";

export const tableStyles = makeStyles({
  tableHeadCell: {
    fontWeight: "bold",
  },
  checkbox: {
    padding: 0,
  },
  sectionTitle: {
    marginTop: 20,
  },
});
